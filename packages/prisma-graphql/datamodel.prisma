type User {
  id: ID! @unique
  name: String!
  email: String! @unique
  password: String!
  status: Boolean @default(value: "true")
}

type Menu {
  id: ID! @unique
  url: String!
  title: String!
  icon: String
  authorization: String
}
